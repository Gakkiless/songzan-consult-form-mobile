# PRD：松赞销售工作台 — 客户维护与待办

> 版本：v4.0 | 日期：2026-04-18 | 原型：`工作台-客户维护与待办-原型.html`
>
> 变更说明：……v3.8 全页面移除兴趣标签和客户类型：待办筛选器仅保留状态筛选（去除客户类型+兴趣标签两组筛选），待办卡片标签行仅保留出行人关系+跟进状态，维护记录筛选栏移除客户类型和兴趣标签两个下拉，维护记录表格移除「标签信息」列，会员查询列表移除客户类型标签和匹配率展示；v4.0 布局重构：整体布局由上下堆叠改为左右分栏（左侧38%待办区域、右侧62%维护记录区域），移除独立统计概览条，「名下会员数」和「本月维护次数」合并至维护记录标题栏右侧展示，快捷入口按钮加大。

---

## 一、需求概述

### 1.1 背景

松赞会员中台会员卡详情页原有「维护记录及事项提醒」模块（v1.0），存在以下问题：

- **职责混淆**：销售员和会员运营共用会员卡详情页，工作场景交叉
- **操作链路长**：销售员跟进客户需先进详情页 → 找到模块 → 才能记录
- **待办分散**：事项提醒散落在各个会员详情页中，无法全局查看
- **效率低下**：缺乏批量处理待办的能力
- **信息割裂**：客户资产看板（客户类型、标签、跟进状态）与维护记录系统独立，销售员跟进时无法直接看到客户画像信息（注：客户画像字段已于v3.8简化，留待后续迭代逐步引入）

### 1.2 目标

将维护记录从会员卡详情页剥离，与首页待办合并为独立**松赞销售工作台**，同时融合客户资产看板的客户画像数据，实现：

- **一站式跟进**：销售员在工作台即可完成查看待办、处理事项、新增/编辑维护记录的全流程
- **职责分离**：会员运营专注于会员卡详情页的信息管理，销售员专注于工作台的客户跟进
- **左右分栏**：待办和维护记录同屏展示，一屏内完成「看待办 + 查记录」的高效工作流
- **快捷操作**：提供新建咨询单、会员查询等高频操作的快捷入口（加大按钮，便于快速点击）

### 1.3 页面定位

| 页面 | 使用角色 | 核心职责 |
|------|----------|----------|
| 会员卡详情页 | 会员运营 | 会员信息管理（基础信息、字段编辑、卡操作） |
| **松赞销售工作台** | **销售员** | **客户跟进（维护记录、待办处理、事项提醒、客户画像）** |

> 会员卡详情页保留「查看」维护记录的入口（只读），但不再提供新增/编辑能力。新增/编辑统一在工作台操作。

### 1.4 入口

- **独立页面**：顶部品牌栏「松赞销售工作台」，直接访问
- **Badge角标**：待办数量通过顶栏或企业徽标展示

---

## 二、页面结构

### 2.1 整体布局（左右分栏）

```
┌─────────────────────────────────────────────────┐
│ 顶栏：松赞销售工作台                          赵岍│
├─────────────────────────────────────────────────┤
│ 快捷入口：[+ 新建咨询单] [🔍 会员查询及维护]      │
├────────────────────────┬────────────────────────┤
│ 📋 我的待办            │ 📝 维护记录 会员156 维护42│
│ [状态筛选]              │        [导出] [操作日志]  │
│                        │ [筛选栏] 会员|行为|日期   │
│ ┌──────────────────┐  │ ┌──────────────────────┐│
│ │ 待办卡片          │  │ │会员卡号│更新时间│会员  ││
│ └──────────────────┘  │ │维护日期│行为│反馈│产出 ││
│ ┌──────────────────┐  │ │        操作            ││
│ │ 待办卡片          │  │ └──────────────────────┘│
│ └──────────────────┘  │ [分页]                    │
│ ┌──────────────────┐  │                          │
│ │ 待办卡片          │  │                          │
│ └──────────────────┘  │                          │
│ ...（可滚动）           │                          │
├────────────────────────┴────────────────────────┤
│                  （弹窗）                         │
│  🔍 会员查询弹窗 / 新增维护记录弹窗 / 操作日志弹窗 │
└─────────────────────────────────────────────────┘
```

**分栏比例**：左侧待办区域占 38%，右侧维护记录区域占 62%。左侧待办卡片列表超出可视区域后可纵向滚动。

### 2.2 顶栏

| 元素 | 说明 |
|------|------|
| 品牌标识 | 左上角「松赞销售工作台」 |
| 用户信息 | 右上角显示当前用户头像和名称（如：喵喵） |

### 2.3 快捷入口条

顶栏下方横向排列高频操作入口，按钮加大便于点击：

| 入口 | 说明 |
|------|------|
| + 新建咨询单 | 跳转咨询单新建页面（蓝色主按钮） |
| 🔍 会员查询及维护 | 打开会员查询弹窗（详见 2.4） |

> **会员卡详情**不再作为快捷入口。待办卡片、维护记录表格、会员查询详情面板中的**会员姓名**均为可点击链接，点击即跳转至会员中台会员卡详情页。

### 2.4 会员查询及维护弹窗

点击快捷入口「🔍 会员查询及维护」打开弹窗，支持全量会员搜索和详情查看。

#### 2.4.1 搜索功能

| 功能 | 说明 |
|------|------|
| 搜索输入 | 单行输入框，支持按**姓名**、**会员卡号**、**手机号**模糊搜索 |
| 触发方式 | 点击「搜索」按钮或按 Enter 键触发 |
| 结果展示 | 列表形式，每条展示：头像、姓名、出行人关系标签、卡号、卡等级、手机号、归属销售 |
| 无归属标记 | 搜索结果中，归属销售为空的会员显示红色「⭕ 暂无归属」标识 |

> **交互流程**：输入关键词搜索后，弹窗展示会员列表；点击列表中的某个会员后，列表收起，下方展开该会员的详情面板；详情面板顶部显示「← 返回搜索结果」链接，点击可回到列表继续选择其他会员。

#### 2.4.2 会员详情面板

点击搜索结果中的任意会员，**搜索结果列表收起**，该会员的详情面板在弹窗内展开，展示完整会员画像。详情面板上方提供「← 返回搜索结果」链接，点击后详情收起、列表恢复显示。

**头部信息**（渐变背景）：

| 字段 | 类型 | 说明 |
|------|------|------|
| 头像 + 姓名 | 大头像 + 文字（**可编辑**） | 姓名右侧显示 ✏️ 图标，点击进入内联编辑态，输入框回车或点「保存」生效，修改写入字段变更日志 |
| 卡号 + 卡等级 | 只读 | 如「MD80001234 · 金刚会员」 |
| 归属销售标签 | 只读 | 已有销售显示「👤 归属销售：XXX」；无归属显示红色「⭕ 暂无归属销售 — 可认领」 |

**基本信息**（两列网格，一期仅展示以下字段）：

| 字段 | 类型 | 说明 |
|------|------|------|
| 手机号码 | 只读 | 脱敏展示，来自会员卡详情页 |
| 会员所在地 | **可编辑** | 右侧显示 ✏️ 图标，点击进入内联编辑态，保存后写入字段变更日志 |
| 累计消费 | 只读 | 来自客户资产看板，红色高亮 |
| 最近下单 | 只读 | 来自客户资产看板 |
| 会籍有效期 | 只读 | 会员卡有效期 |
| 最近联系 | 只读 | 最近一次维护记录的时间和方式 |

> 一期暂不展示「客户类型」「跟进状态」「标签信息（兴趣/出行人关系/个人偏好）」「标签匹配率」，留待后续迭代引入。

**会员关系**：展示关联会员列表（配偶、子女等），每条包含：关联会员姓名（可点击跳转）、关系类型、卡号+卡等级。无关联时显示「暂无关联会员」。

**字段变更日志**（内联，仅当本次会话发生过字段修改时展示）：

| 字段 | 说明 |
|------|------|
| 展示时机 | 本次打开弹窗后，有任意字段被修改保存后自动展示；切换会员后清空 |
| 记录内容 | 时间（HH:mm:ss）+ 字段名 + 旧值 → 新值 + 操作人 |
| 顺序 | 最新修改排在最前 |
| 样式 | 旧值加删除线灰色展示，新值正常展示，时间+操作人弱色 |

> **后端落地说明**：字段变更日志需持久化到服务端，记录格式：`{ memberId, field, oldValue, newValue, operator, operateAt }`，同时需同步写入 CDP 用户属性更新日志。

#### 2.4.3 操作按钮

详情面板底部提供两个操作按钮：

| 按钮 | 样式 | 条件 | 说明 |
|------|------|------|------|
| 📥 纳入我的客户 | 绿色 | 仅无归属会员可点击 | 将该会员认领到当前销售员名下 |
| ＋ 新增维护记录 | 蓝色（主按钮） | 始终可点击 | 关闭查询弹窗，打开维护记录新增弹窗（自动定位到该会员） |

#### 2.4.4 认领逻辑

- **认领条件**：仅「暂无归属销售」的会员可被认领
- **认领操作**：点击「纳入我的客户」按钮，二次确认后生效
- **认领后**：
  - 会员归属销售更新为当前登录销售员
  - 该会员自动出现在销售员的待办和维护记录中
  - 写入操作日志（类型：认领会员）
  - 实时同步CDP
  - 按钮变为灰色「✓ 已有归属销售」不可再次点击
- **限制**：已被其他销售员认领的会员，当前销售员不可抢认

---

## 三、客户画像数据（融合客户资产看板）

### 3.1 数据来源

以下字段来源于客户资产看板系统，在工作台中作为**只读展示**和**筛选维度**使用：

| 字段 | 来源 | 说明 |
|------|------|------|
| 客户类型 | 客户资产看板 | 基于消费金额和活跃度自动分级 |
| 兴趣标签 | 客户资产看板 | 基于消费记录和咨询偏好自动标注 |
| 跟进状态 | 客户资产看板 | 基于最近联系天数自动判定 |
| 最近联系天数 | 客户资产看板 | 距上一次维护记录的天数 |
| 累计消费金额 | 客户资产看板 | 会员历史总消费金额 |
| 最近下单日期 | 客户资产看板 | 会员最近一次下单日期 |

### 3.2 客户类型定义

与客户资产看板保持一致：

| 类型 | 分级标准 | 标签样式 |
|------|----------|----------|
| **VIP客户** | 累计消费 ≥ 30万 | 红底 #FFF1F0 红字 #CF1322 |
| **重点客户** | 累计消费 10-30万 | 橙底 #FFF7E6 橙字 #AD6800 |
| **活跃客户** | 最近30天有消费或互动 | 蓝底 #E6F7FF 蓝字 #096DD9 |
| **普通客户** | 最近90天有互动 | 灰底 #F5F5F5 灰字 #666 |
| **沉睡客户** | 超过90天无任何互动 | 浅灰底 #F0F0F0 深灰字 #999 |

### 3.3 兴趣标签枚举

与客户资产看板保持一致，共8种：

| 标签 | 图标 | 说明 |
|------|------|------|
| 自然风光 | 🏔️ | 偏好雪山、冰川、草原等自然景观路线 |
| 人文历史 | 🏛️ | 偏好寺庙、古镇、文化遗产等人文体验 |
| 奢享度假 | 💎 | 偏好高端定制、私厨、套房等奢享服务 |
| 亲子出行 | 👨‍👩‍👧 | 有家庭出游需求，偏好亲子友好的行程 |
| 美食体验 | 🍜 | 对藏地美食、特色餐饮有兴趣 |
| 户外徒步 | 🥾 | 偏好徒步、露营等户外活动 |
| 摄影采风 | 📷 | 摄影爱好者，关注日出日落、星空等拍摄点 |
| 雪山温泉 | ♨️ | 偏好温泉度假、雪山观景体验 |

> 标签样式统一为紫底圆角胶囊：`#F9F0FF` / `#722ED1`

### 3.4 出行人关系标签

描述会员常见的出行组合方式，帮助销售员匹配产品推荐策略：

| 标签 | 样式 | 说明 |
|------|------|------|
| 夫妻出行 | 青色胶囊 `#E6FFFB` / `#08979C` | 两人结伴出行，偏好双人间 |
| 亲子出行 | 青色胶囊 | 携带儿童出行，需要亲子友好行程和设施 |
| 父母同游 | 青色胶囊 | 与父母长辈一起出行，需关注行程舒适度 |
| 朋友结伴 | 青色胶囊 | 与朋友一起出行，3-6人小团为主 |
| 闺蜜出行 | 青色胶囊 | 与闺蜜一起出行，偏好拍照打卡和美食体验 |
| 独自出行 | 青色胶囊 | 独自一人出行，偏好灵活自由行或拼团 |

### 3.5 个人偏好标签

描述会员的个人特质和特殊需求，帮助销售员精细化服务：

| 标签类别 | 示例 | 样式 |
|----------|------|------|
| 饮食偏好 | 素食偏好、忌辣、忌牛羊肉 | 橙色胶囊 `#FFF7E6` / `#AD6800` |
| 高原经验 | 高原经验丰富、无高原经验 | 橙色胶囊 |
| 预算特征 | 预算敏感 | 橙色胶囊 |
| 特殊需求 | 儿童餐需求、摄影器材需求、自带装备、私厨定制、红酒偏好、露营经验 | 橙色胶囊 |

> 出行人关系标签和个人偏好标签由销售员在会员卡详情页中手动维护，工作台只读展示。

### 3.6 标签匹配率

| 指标 | 说明 |
|------|------|
| 定义 | 基于会员兴趣标签推荐对应产品后，会员实际下单的转化率 |
| 计算方式 | 分标签计算：某标签推荐次数中，会员实际下单的占比；总匹配率为各标签匹配率的加权平均 |
| 更新时机 | 每次会员完成订单支付后，系统自动更新匹配率 |
| 展示位置 | 待办卡片标签行、维护记录表格标签列、会员查询详情面板 |
| 颜色规则 | ≥ 70% 绿色（高匹配）；50%-69% 黄色（中匹配）；< 50% 红色（低匹配） |
| 业务含义 | 匹配率高说明销售推荐精准，会员信任度好；匹配率低说明推荐与实际需求存在偏差，需调整策略 |

### 3.4 跟进状态定义

| 状态 | 判定条件 | 标签样式 |
|------|----------|----------|
| **需跟进** | 最近联系天数 > 7天 | 红底 `#FFF1F0` 红字 `#F5222D` |
| **即将到期** | 最近联系天数 3-7天 | 橙底 `#FFF7E6` 橙字 `#FA8C16` |
| **正常** | 最近联系天数 ≤ 3天 | 绿底 `#F6FFED` 绿字 `#52C41A` |

---

## 四、功能详述

### 4.1 统计指标

v4.0 移除独立统计概览横条，精简为仅保留「名下会员数」和「本月维护次数」两个核心指标，展示于维护记录区域标题栏右侧：

| 指标 | 说明 | 展示位置 |
|------|------|----------|
| 名下会员数 | 当前登录销售员名下的会员总数 | 维护记录标题栏右侧，绿色数字 |
| 本月维护次数 | 当月新增维护记录总数 | 维护记录标题栏右侧，蓝色数字 |

> 已过期待办数、今日到期数、待处理数、需跟进客户数已通过待办筛选标签体现（全部16、已过期5、今日到期3、即将到期8），无需在统计条重复展示。

### 4.2 我的待办

#### 4.2.1 筛选标签

**状态筛选**（一期仅保留此组筛选）：

| 标签 | 筛选逻辑 |
|------|----------|
| 全部 | 所有未完成的待办 |
| 已过期 | 提醒日期 < 今天 |
| 今日到期 | 提醒日期 = 今天 |
| 即将到期 | 明天 ≤ 提醒日期 ≤ 7天内 |

> ~~客户类型筛选和兴趣标签筛选~~ 已于v3.8移除，留待后续迭代引入。原两组筛选器可叠加使用的设计保留，后续恢复时按「状态 + 客户类型 + 兴趣标签」组合筛选。

#### 4.2.2 待办卡片

每条待办以卡片形式展示，按优先级排序（已过期 → 今日到期 → 即将到期 → 更远），同优先级内按截止时间升序。卡片在左侧分栏中**单列纵向排列**，超出可视区域后可滚动查看。

**卡片结构**：

| 区域 | 内容 |
|------|------|
| 头部左侧 | ⭕ 完成勾选框 |
| 头部左侧 | 会员姓名 + 卡号 + 卡等级 |
| 头部右侧 | 状态标签 |
| **标签行** | **客户类型标签 + 兴趣标签 + 跟进状态标签** |
| 内容区 | 事项内容（产出结果/跟进计划） |
| 底部左侧 | 来源记录 + 截止日期 + **累计消费金额** |
| 底部右侧 | 操作按钮 |

**卡片标签行说明**（v3.8简化后）：

| 标签 | 位置 | 说明 |
|------|------|------|
| 出行人关系 | 标签行第一位 | 如「夫妻出行」「亲子出行」，青色胶囊 |
| 跟进状态 | 标签行末尾 | 如「需跟进」「正常」，颜色胶囊 |

> ~~客户类型、兴趣标签、标签匹配率~~ 已于v3.8从待办卡片标签行移除，留待后续迭代引入。

**卡片底部元信息**：

在原有「来源 + 截止日期」基础上，新增**累计消费金额**，帮助销售员判断客户价值。

**卡片样式**：

| 状态 | 左边框 | 背景 |
|------|--------|------|
| 已过期 | 红色 #F5222D | #FFF1F0 |
| 今日到期 | 橙色 #FA8C16 | #FFF7E6 |
| 即将到期 | 蓝色 #1890FF | 白色 |

#### 4.2.3 完成待办

- 点击卡片左上角的 ⭕ 勾选框，将事项标记为「已完成」
- 已完成的待办从列表中移除，不再展示
- 操作记入维护记录的操作日志

#### 4.2.4 标记完成

- 点击卡片底部的「✓ 标记完成」按钮，将事项标记为「已完成」
- 卡片带淡出动画从列表中移除，不再展示
- 操作记入维护记录的操作日志

---

### 4.3 维护记录表格

#### 4.3.1 表格结构

| 列名 | 宽度 | 类型 | 说明 |
|------|------|------|------|
| 会员卡号 | 130px | 文本 | 会员MD卡号，直接从会员中台获取 |
| 更新时间 | 145px | 日期时间（系统自动） | 格式 `YYYY-MM-DD HH:mm:ss`，创建时等于当前时间，每次编辑后自动刷新 |
| 会员 | 180px | 会员信息（姓名可点击） | 展示头像+**姓名（可点击跳转会员卡详情页）**+手机号脱密+卡等级 |
| 维护日期 | 90px | 日期（手动选择） | 格式 `YYYY-MM-DD`，允许选择任意日期 |
| 维护行为 | 75px | 标签枚举 | 线下拜访 / 电话回访 / 活动邀约 / 微信沟通 |
| 客户反馈 | 自适应 | 多行文本 | 记录客户关键反馈 |
| 产出结果/跟进计划 | 自适应 | 多行文本 | 记录本次达成共识或下一步计划 |
| 操作 | 90px | 按钮 | 查看 / 编辑 |

> ~~标签信息列（客户类型+跟进状态+兴趣标签+出行人关系+标签匹配率）~~ 已于v3.8移除，留待后续迭代引入。

#### 4.3.2 维护行为枚举

| 枚举值 | 标签样式 | 背景色 | 文字色 |
|--------|----------|--------|--------|
| 线下拜访 | 橙色标签 | #FFF7E6 | #AD6800 |
| 电话回访 | 青色标签 | #E6FFFB | #08979C |
| 活动邀约 | 粉色标签 | #FFF0F6 | #C41D7F |
| 微信沟通 | 绿色标签 | #F6FFED | #389E0D |

> 维护行为选项由系统统一管理，销售员不可新增/修改枚举选项。

#### 4.3.3 筛选栏

| 筛选项 | 类型 | 说明 |
|--------|------|------|
| 会员 | 搜索框 | 支持按姓名、卡号模糊搜索 |
| 维护行为 | 下拉选择 | 全部 / 线下拜访 / 电话回访 / 活动邀约 / 微信沟通 |
| 维护日期 | 日期范围 | 起止日期选择器 |

> ~~客户类型下拉和兴趣标签下拉~~ 已于v3.8移除，留待后续迭代引入。

#### 4.3.5 排序与分页

- 默认按**更新时间倒序**排列
- 支持点击表头切换排序字段
- 默认每页 20 条，可切换 50条/页、100条/页
- 展示总记录数

---

### 4.4 新增维护记录

#### 4.4.1 触发方式

**唯一入口**：在会员查询弹窗（详见 2.4）中，选中某个会员后，点击详情面板底部的「＋ 新增维护记录」按钮。系统自动关闭查询弹窗，打开维护记录新增弹窗，并将当前选中会员的信息带入弹窗顶部（只读展示，不可修改）。

> 维护记录表格区域不再提供「新增」按钮，所有新增维护记录操作必须先通过会员查询定位到具体会员。

#### 4.4.2 弹窗表单字段

弹窗顶部**只读展示**当前会员信息（由会员查询面板自动带入）：

| 展示项 | 说明 |
|--------|------|
| 头像+姓名 | 会员头像 + 姓名 |
| 卡号 · 卡等级 · 客户类型 | 如「MD80001234 · 金刚 · VIP客户」 |

> 会员信息为只读，不可在弹窗中切换或修改。

**表单字段**：

| 字段 | 必填 | 类型 | 说明 |
|------|:---:|------|------|
| 维护日期 | ✅ | 日期选择器 | 默认当天，允许选择任意历史日期 |
| 维护行为 | ✅ | 下拉选择 | 线下拜访 / 电话回访 / 活动邀约 / 微信沟通 |
| 客户反馈 | ❌ | 多行文本 | placeholder: 记录客户关键反馈、态度或需求... |
| 产出结果/跟进计划 | ❌ | 多行文本 | placeholder: 记录本次达成的共识、下一步计划或具体成果... |

#### 4.4.3 事项提醒（非必选，开关控制）

表单底部、提交按钮上方，以虚线分隔展示「⏰ 设置事项提醒」开关区域：

- **开关**：默认关闭。开启后展开提醒设置字段，关闭时隐藏
- **提醒日期**（日期选择器）：到期后自动在工作台生成一条待办事项
- **提醒事项**（多行文本）：待办卡片中展示的事项内容，如未填写则自动取「产出结果/跟进计划」

> 设置提醒后，系统将根据提醒日期在工作台「我的待办」中自动生成一条待办。提醒日期 < 今天为「已过期」，= 今天为「今日到期」，7天内为「即将到期」。超出7天的待办不展示。

#### 4.4.4 出行需求信息（非必选）

弹窗下半区以虚线分隔展示「📋 出行需求信息」区域，全部非必选、单选下拉：

| 字段 | 类型 | 选项 |
|------|------|------|
| 出行时节 | 单选下拉 | 请选择 / 春季（3-5月） / 夏季（6-8月） / 秋季（9-11月） / 冬季（12-2月） |
| 出行组合 | 单选下拉 | 请选择 / 独自出行 / 情侣出行 / 亲子出行 / 朋友结伴 / 父母同游 / 大家庭 |
| 人均预算 | 单选下拉 | 请选择 / 5,000以下 / 5,000-10,000 / 10,000-20,000 / 20,000-50,000 / 50,000以上 |
| 出行天数 | 单选下拉 | 请选择 / 3天以内 / 4-6天 / 7-10天 / 11-15天 / 15天以上 |
| 出行偏好 | 单选下拉 | 请选择 / 自然风光 / 人文历史 / 奢享度假 / 户外徒步 / 摄影采风 / 亲子互动 / 美食体验 / 温泉养生 |

> 出行需求信息在销售员维护客户时可选填，帮助积累会员出行偏好数据，后续可用于精准推荐。

#### 4.4.5 提交逻辑

1. 校验维护日期（必填）和维护行为（必填）
2. 会员信息由会员查询面板自动带入，无需手动选择
3. 更新时间 = 创建时间 = 当前系统时间（格式 YYYY-MM-DD HH:mm:ss）
3. 写入操作日志（类型：新增）
4. **实时同步CDP**
5. Toast 提示「维护记录已添加并同步至CDP」
6. 刷新表格和待办列表

---

### 4.5 编辑维护记录

#### 4.5.1 触发方式

- 点击表格操作列的「编辑」链接

#### 4.5.2 编辑逻辑

1. 打开编辑弹窗（复用新增弹窗，预填已有数据，会员不可切换）
2. 所有字段均可编辑（维护日期、维护行为、客户反馈、产出结果）
3. **更新时间自动刷新为当前系统时间**
4. 写入操作日志（类型：修改，记录变更内容摘要）
6. **实时同步CDP**
7. 刷新表格和待办列表

---

### 4.6 系统自动催办

#### 4.6.1 催办机制

- **扫描频率**：每日 09:00 定时任务自动扫描所有未完成且已过期的维护事项
- **催办对象**：该会员对应的**销售员**
- **催办渠道**：企业微信消息推送 + 工作台待办列表
- **催办频率**：每天推送一次，直到事项被标记为「已完成」或手动取消提醒

#### 4.6.2 催办通知内容

| 通知字段 | 说明 |
|----------|------|
| 会员 | 会员姓名 + 卡号 |
| 客户类型 | VIP客户/重点客户等（帮助销售员判断优先级） |
| 提醒事项 | 该条维护记录的产出结果/跟进计划内容 |
| 来源记录 | 维护日期 + 维护行为 |
| 截止时间 | 原设定的提醒日期 |
| 过期天数 | 距截止日已过期的天数 |

---

### 4.7 操作日志

#### 4.7.1 触发方式

点击维护记录区域标题栏的「📋 操作日志」按钮，弹窗展示。

#### 4.7.2 日志表格结构

| 列名 | 宽度 | 说明 |
|------|------|------|
| 更新时间 | 150px | 格式 YYYY-MM-DD HH:mm:ss |
| 更新人 | 70px | 操作人姓名 |
| 变更类型 | 60px | 新增（绿色）/ 修改（蓝色）/ 删除（红色） |
| 会员 | 100px | 关联的会员姓名，便于定位 |
| 维护日期 | 90px | 关联的维护记录日期 |
| 变更内容摘要 | 自适应 | 自然语言描述变更内容 |

#### 4.7.3 日志记录规则

| 操作类型 | 记录内容 |
|----------|----------|
| 新增记录 | 新增维护记录：{会员姓名} — {维护行为} — {客户反馈摘要} |
| 修改记录 | 修改【{会员姓名}】【{字段名}】：{修改说明} |
| 完成待办 | 完成【{会员姓名}】事项提醒 |
| 删除记录 | 删除维护记录：{会员姓名} — {维护行为} — {维护日期} |

> 日志不可删除、不可编辑，仅追加。

---

### 4.8 导出

点击维护记录区域标题栏的「📥 导出」按钮，将当前筛选结果导出为 Excel 文件，字段包含：会员卡号、更新时间、会员姓名、手机号、客户类型、兴趣标签、维护日期、维护行为、客户反馈、产出结果/跟进计划、出行需求信息。

---

### 4.9 会员卡详情页的维护记录入口（只读）

会员卡详情页保留维护记录的**只读查看入口**：

- **位置**：会员卡详情页表单区底部（原v1.0位置），折叠面板形式
- **展示内容**：该会员的全部维护记录（只读表格），字段与工作台一致
- **操作**：点击任意记录的「查看详情」跳转至工作台（带会员筛选参数）
- **新增/编辑**：详情页不提供，引导「前往工作台操作」

---

## 五、数据流转

### 5.1 CDP 同步

所有维护记录的**新增、修改、删除**操作，均需实时同步至腾讯营销云 CDP，同步字段：

- 会员标识（卡号/手机号）
- 维护日期
- 更新时间（格式 YYYY-MM-DD HH:mm:ss）
- 维护行为
- 客户反馈
- 产出结果/跟进计划
- 出行需求信息（出行时节/出行组合/人均预算/出行天数/出行偏好，选填）
- 最近联系天数（更新为0，重新计算）

### 5.2 客户资产看板数据同步

维护记录新增/编辑后，以下客户资产看板字段自动更新：

| 字段 | 更新逻辑 |
|------|----------|
| 最近联系天数 | 重置为0，每日定时任务自动累加 |
| 跟进状态 | 根据最新「最近联系天数」重新判定 |
| 客户类型 | 如有新消费数据则重新评估 |

### 5.3 企业微信通知

| 触发场景 | 通知对象 | 通知内容 |
|----------|----------|----------|
| 事项提醒到期 | 销售员 | 到期事项详情（会员信息、客户类型、提醒事项、截止时间） |
| 事项提醒过期（系统自动催办） | 销售员 | 过期事项详情（系统每日09:00自动推送） |
| 工作台待办更新 | 销售员 | 工作台待办列表实时更新 |

---

## 六、权限设计

### 6.1 维护记录操作权限（仅销售员角色）

| 操作 | 权限 |
|------|:---:|
| 新增维护记录（本人名下会员） | ✅ |
| 编辑维护记录（本人创建） | ✅ |
| 完成待办（本人） | ✅ |
| 查看本人数据 | ✅ |
| 系统催办（自动） | ✅ |
| 导出本人数据 | ✅ |
| 管理维护行为选项 | ❌ |

### 6.2 客户画像数据权限

| 操作 | 权限 | 说明 |
|------|:---:|------|
| 查看客户类型/标签（本人名下） | ✅ | — |
| 修改客户类型/标签 | ❌ | 仅客户资产看板系统可修改 |

### 6.3 字段编辑权限

| 字段 | 可编辑角色 | 说明 |
|------|-----------|------|
| 销售员 | 仅创建时录入 | 录入后不可修改 |
| 城市使者 | 指定权限 | 会员卡详情页操作 |
| 会员所在地 | 指定权限 | 会员卡详情页操作 |
| 会籍到期日 | 指定权限 | 会员卡详情页操作 |
| 姓名 | 未实名时可录入 | 会员卡详情页操作 |

> **后续迭代预留**：销售负责人和管理员角色将在后续版本中设计，主要涉及统计看板和大区数据聚合视图。

---

## 七、非功能需求

| 项目 | 要求 |
|------|------|
| 性能 | 表格加载 ≤ 1秒，待办列表加载 ≤ 0.5秒，单页展示 20 条 |
| 数据保留 | 维护记录永久保留，操作日志保留 2 年 |
| 并发 | 支持同一会员被多人查看，编辑时加乐观锁 |
| 可用性 | CDP 同步失败不影响本地写入，异步重试 3 次 |
| 客户画像 | 客户类型/标签数据从客户资产看板系统实时获取，延迟 ≤ 1秒 |

---

## 八、与v1.0差异对照

| 项目 | v1.0（会员卡详情页内） | v4.0（松赞销售工作台） |
|------|----------------------|----------------------|
| 页面位置 | 会员卡详情页底部 | 独立工作台页面 |
| 页面布局 | 无（模块嵌入详情页） | **左右分栏**（左侧38%待办+右侧62%维护记录） |
| 使用角色 | 销售员 + 会员运营共用 | 销售员（负责人/管理员后续迭代） |
| 待办展示 | 无，需逐个会员查看 | 顶部卡片集中展示 |
| 客户画像 | 无 | 待办卡片展示出行人关系+跟进状态（v3.8简化） |
| 标签体系 | 仅兴趣标签 | 三类标签：兴趣标签 + 出行人关系标签 + 个人偏好标签 |
| 标签匹配率 | 无 | 基于兴趣标签推荐→下单转化率，分标签展示 |
| 会员查询 | 无 | 弹窗模糊搜索（姓名/卡号/手机号），选中后列表收起展开详情，支持返回列表切换，无归属会员认领 |
| 会员认领 | 无 | 「纳入我的客户」一键认领无归属会员 |
| 会员预览 | 无 | 新增弹窗中选择会员后展示画像预览 |
| 多维筛选 | 无 | 支持按待办状态筛选（v3.8简化） |
| 统计概览 | 无 | 名下会员数+本月维护次数（维护记录标题栏右侧） |
| 新增流程 | 进详情页 → 找模块 → 新增 | 会员查询 → 选中会员 → 点击「新增维护记录」（会员自动带入） |
| 编号维度 | 按会员自增 | 已移除，改为独立「会员卡号」列 |
| 快捷入口 | 无 | 新建咨询单、会员查询 |
| 会员卡详情入口 | 详情页模块内 | 点击会员姓名即跳转（待办/表格/查询面板均支持） |
| 详情页保留 | — | 只读查看 + 跳转工作台 |
| 数据范围 | 当前查看的会员 | 仅当前登录销售员本人 |
| 客户资产联动 | 无 | 维护记录更新自动同步最近联系天数和跟进状态 |
